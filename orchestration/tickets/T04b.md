# Ticket T04b (from PLAN.md — authoritative)

### T04b — `bin/omafan-ctl` amendment: the `cycle` verb
- **Goal:** add the one verb DESIGN §4/§4.5 requires that your delivery does not
  implement. Everything else you shipped was accepted by the orchestrator
  (`status`, `presets`, `doctor`, `preset`, `rpm`, `release`, `--dry-run`,
  `--pkexec none`, exit codes 0/2/4/5/6/7/8, the §4.1-§4.4 documents and the §5.1
  undercooling guard) — do not regress any of it.
- **Files (own):** `bin/omafan-ctl`.
- **Spec (DESIGN.md §4.5, frozen):** `omafan-ctl cycle [--notify] [--dry-run]
  [--json] [globals]`.
  - Current preset = `hold.preset` when the daemon's applied mode is `hold`
    (same derivation as §4.1, `custom` counts: a hold at a non-preset rpm cycles
    to `off`), else `auto`.
  - Next = one step forward in the §3 order with wraparound
    (`auto → off → low → med → high → full → auto`), i.e. exactly
    `Model.cyclePreset(current, +1)` (read `Model.js` — do not re-derive the order).
  - Apply it exactly as `preset <next>`: same validation, same §5.1 undercooling
    guard (exit 8, `--force` overrides), same §4.3 JSON but with
    `"action": "cycle"` and `"preset": "<next>"`.
  - `--dry-run` prints the argv and the chosen next preset without executing.
- **Also (from the orchestrator's verification):** mirror the §4.1 R6 clarification
  in the `--help` text — `status` prints a document even when the daemon is down
  and exits 5 in that case, while a write verb exits 5 without writing. Make the
  help text say so in one line so the contract is discoverable without the docs.
- **Gates:** `bash -n bin/omafan-ctl`; then, against the fixture
  (`--afanctl tests/fixtures/fake-afanctl --pkexec none --runtime-dir <tmp>` with a
  seeded `state.json`):
  `cycle` from `auto` → applies `off 1200`; from `off` → `low 2700`; from a
  `custom` hold (e.g. 4321 rpm) → `off`; from `full` → `auto` (i.e. `observe`,
  argv `observe`, no rpm); `--dry-run` writes nothing to the fixture's `argv.log`;
  `cycle` on a monitor-only state exits 6; on a hot state (t_eff 90, rpm 4794)
  `cycle` into `low` exits 8 and `--force` overrides; `-h` lists `cycle`.
  Paste all nine results in your report.
- **Done:** `bash -n` clean; the nine transcripts above; `--help` line added.
