# Ticket T05b (from PLAN.md — authoritative)

### T05b — `tests/ctl.test.sh` update for the T04d write-path fix (4 failures)
- **Goal:** your suite is 187 PASS / 4 FAIL after T04d fixed the privileged write
  path. All four failures are **assertions that encode the old, broken behaviour**;
  update them and add the coverage T04d needs so this class of defect can never
  come back green by accident.
- **Files (own):** `tests/ctl.test.sh`.
- **The four failures (orchestrator's run, on this machine):**
  1. `doctor emits the eleven fixed checks: expected [11], got [12]`
  2. `the check id set is exactly section 4.4: … got […,pkexec_write_path,…]`
  3. + 4. `run_denied preset med exited 2, expected 3`
- **Required updates:**
  - The doctor check id set is now **twelve**: DESIGN §4.4's eleven plus
    `pkexec_write_path` (added by T04d; assert its presence and that its
    `status` is one of PASS/WARN/FAIL).
  - `run_denied` now exits **2** before any runner call, because T04d refuses a
    custom `--runtime-dir` together with a real runner (pkexec cannot carry one).
    Split the case in two: (a) custom runtime dir + real runner → exit **2**, and
    assert **no runner was invoked** (your pkexec stub must not have run — prove
    it with a sentinel file the stub would have touched); (b) a runner that
    *fails* while the runtime dir is the default → exit **3** (authorisation
    denied), which is the behaviour the old assertion was reaching for.
- **Required new assertions (this is the real point of the ticket):**
  - **No `env` wrapper:** for every write verb, the runner argv is exactly
    `[<runner>, <afanctl>, <verb> (, <rpm>)]`; assert `argv[1]` is not `env` and
    has no `VAR=value` shape. Add a stub runner that **exits 42 if `argv[1]` is
    `env`** and assert the write still succeeds — that stub is the regression
    guard for the live blocker (`pkexec env … /usr/bin/afanctl …` is NOT_HANDLED
    by afanctl's polkit rule, so pkexec prompts for a password and hangs).
  - **A runner that never answers** (stub that sleeps 60 s) makes the write exit
    **3** within T04d's bound, and prints the "authorisation timed out" message;
    assert the wall-clock stays under ~25 s so a future edit cannot silently drop
    the bound.
  - **`--dry-run` with a runner** prints the bare argv and executes nothing.
- **Gates:** `bash -n tests/ctl.test.sh`; `bash tests/ctl.test.sh` →
  `PASS n / FAIL 0` (report n); `bash tests/run-all.sh` → all six suites PASS.
  Paste the four rewritten/new assertion results in your report.
- **Done:** the suite is green again and contains the two regression guards.
