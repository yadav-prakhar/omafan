# Ticket T09b (from PLAN.md — authoritative)

### T09b — `Panel.qml` fix round: rendered-text defects
- **Goal:** fix what the orchestrator saw on screen after loading your panel in
  the **live shell** (screenshot transcribed: header `CPU · Fan 6,078 rpm`,
  sub-header `FIRMWARE AUTO`, six preset chips, the floors footnote, the RPM
  slider, footer `Uptime · Polls 4124 · Verified: no`). The layout, the cursor
  model, the banners and the IPC surface all work — keep them.
- **Files (own):** `Panel.qml`.
- **D1 (defect):** the header renders `CPU · Fan 6,078 rpm` — the temperature is
  missing because `thermal.t_eff_c` arrived as `null` on `omafan-ctl`'s fast path.
  Ticket T04c fixes the source; your part is to render honestly when it is still
  missing: show `CPU — · Fan 6,078 rpm` (an em dash, never a fake number, never a
  dangling separator like `CPU · `), and keep the separator style consistent when
  only one of the two values is known.
- **D2 (defect):** the footer reads `Uptime · Polls 4124 · Verified: no`, which is
  misleading twice over: `Uptime` has no value (T04c fixes the value) and
  `Verified: no` reads as a fault when nothing is being held. Rule:
  `Uptime <value> · Polls <n>` always (with `—` for an unknown uptime); append
  `· verified` only while a hold is active **and** the daemon reports
  `verified === true`; append `· not verified` only while a hold is active and it
  reports false; show neither when no hold is active.
- **D3 (polish):** the `off` chip is truncated on screen (`Off (hardware flo…`).
  Render the chip label as `Off (floor)` and keep the full wording where there is
  room — the floors footnote already explains it, so no honesty is lost. Do the
  same for any other chip label that cannot fit the panel's content width.
- **D4 (behaviour, from DESIGN §4.1):** the fast path has no per-sensor list, so
  ask for `--full` when the panel opens and on every tenth poll, and use the
  sensor rows when they are present. Keep the 2 s cadence on the fast path.
- **Gates (paste all of them):** `bash tests/qml-lint.sh` passes; and a short
  table of the three footer/header states you can produce with the fixture
  (no hold; hold verified; hold not verified) as rendered strings, taken from the
  functions/properties that build them.
- **Done:** the lint result and that table; no changes outside `Panel.qml`.
