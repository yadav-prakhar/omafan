# orchestration/LEDGER.md — flight recorder

One line per dispatch, verification, rejection, ruling and commit. Newest first.
Format: `<time> <actor> <event> — <evidence command> — <result>`

- 2026-09-15T03:15Z orchestrator P0 recon complete — `hyprctl binds -j`, `afanctl status --json`, `plugins.omarchy.org/catalog.json`, `omarchy plugin --help`, `omarchy-shell omaplug refresh` — 175 bound chords, afanctl 0.1.0 active in observe, 6 sibling fan plugins surveyed, third-party IPC confirmed working; contracts frozen in DESIGN.md.
- 2026-09-15T03:35Z orchestrator wave1 verified — `omarchy plugin validate .` (FAIL: BarWidget.qml missing, expected until P3), `node tests/model.test.mjs` → PASS 124 / FAIL 0, `bash -n` all shell OK, `tests/qml-lint.sh` → SKIP, fake-afanctl manual transcript (status/hold/observe/below-min/refuse-live-dir) correct — T01, T02, T03 ACCEPTED (T01's validate gate deferred to P3 by design; T03's fixture refuses the live /run/afanctl dir, which is stronger than asked).
- 2026-09-15T03:35Z orchestrator ruling R1 — added DESIGN.md §5.1 undercooling guard + exit code 8 (live finding: machine at 97 °C with firmware at 4794 rpm means presets off/low/med reduce airflow) — new ticket T02b (deepseek) and amendments to T05/T09/T10.
- 2026-09-15T03:36Z orchestrator ruling R2 — afanctl takes its runtime dir from AFANCTL_RUNTIME_DIR (no --runtime-dir flag); T04 card amended.
- 2026-09-15T03:36Z orchestrator wrote tests/manifest.test.sh and tests/run-all.sh (T14, orchestrator-owned) — manifest: PASS 22 / FAIL 3 (3 failures are the not-yet-written BarWidget.qml/Panel.qml/README.md, expected until P3/P4).
- 2026-09-15T03:50Z orchestrator ruling R3 — the plugin-dev guide's `qmllint -I "$OMARCHY_PATH/shell"` cannot resolve `qs.Ui`/`qs.Commons` (no `qmltypes`, and the import path must contain a `qs/` dir); verified by running it against the shipped `omaplug` plugin. `tests/qml-lint.sh` (T01's file, T01 already accepted) was rewritten by the orchestrator to build a shim import root, fail on syntax/unresolved-import/missing-non-qs types, and report the qs.* member warnings instead of failing on them. Verified: syntax-error probe -> exit 1; clean probe -> exit 0.
- 2026-09-15T04:05Z orchestrator wave2a verified — T02b: `node tests/model.test.mjs` PASS 148/FAIL 0 (was 124), live probe of isUndercoolingHot/undercoolingWarning correct (97 °C + 2700 target → true, warning names High); T06: `bash tests/keybindings.test.sh` PASS 50/0, `bin/omafan-keybindings status` on the live machine reports no conflict for all eight chords; T07: BarWidget.qml 170 lines, qmllint clean, no Process/exec (as required); T08: KeyboardHelp.qml 96 lines, qmllint clean. `omarchy plugin validate .` now exits 0 (G1) since BarWidget.qml exists.
- 2026-09-15T04:05Z orchestrator defect report on T06 → T06b dispatched — the emitted binding block called a bare `omafan-ctl`, which is not on PATH (Hyprland exec uses a plain shell; every preset chord would fail at key-press time while looking installed). T06b (glm-5.3-flash high) fixes the resolved absolute helper path, adds install-time guards (missing helper, space/quote in path), a `stale-path` verdict in `status`, and tests for each.
- 2026-09-15T04:05Z orchestrator fixed tests/run-all.sh (T07's report) — `run_suite` was called with the wrong argument order, so the plugin-validate suite was silently skipped and the command list was executed directly; corrected to `run_suite <label> <must-exist> <cmd...>`.
- 2026-09-15T04:06Z orchestrator ruling R4 — Panel.qml resolves `bin/omafan-ctl` via `Qt.resolvedUrl(...)` at runtime and honours OMAFAN_AFANCTL/OMAFAN_RUNTIME_DIR/OMAFAN_PKEXEC for hardware-free testing; array-form `Process` commands only. T09 card amended before dispatch.
- 2026-09-15T04:06Z orchestrator ruling R5 — DESIGN §4 gained the `cycle` verb (§4.5) because the frozen keybinding table and the panel's `c` key both need it and §4 did not list it; T04 card carries the requirement, T05's tests must cover it.
- 2026-09-15T04:00Z orchestrator wave3 verified — T04: all exit codes exercised by hand against the fixture (0 ok; 2 unknown preset; 4 afanctl missing; 5 daemon down incl. the R6 read/write split; 6 monitor-only; 7 rpm out of band; 8 undercooling + `--force` override), presets ladder 1200/2700/4200/5700/7200, doctor 9 PASS/2 WARN/0 FAIL, dry-run leaves the fixture argv.log empty. T04b: `cycle` correct from auto/off/custom/full and the hot path (exit 8, force overrides). T06b: keybindings suite PASS 71/FAIL 0; mutation test (swapping X for Q) FAILS the suite, so the tests have signal. T09: qml-lint PASS on 3 files, `omarchy plugin validate .` exit 0, Panel.qml 811 lines. T12: 6 docs written. T13: CHANGELOG + PUBLISHING.
- 2026-09-15T04:01Z orchestrator LIVE integration (PRD G4/G5) — `orchestration/live-install.sh install` (shell.json backed up first) → `omarchy-shell shell rescanPlugins` → `omarchy plugin enable io.github.yadav-prakhar.omafan --section right` → `omarchy plugin list --json` shows enabled:true; `omarchy-shell omafan state` returns a valid `omafan.status.v1` document with the live fan rpm and the preset ladder; `omarchy-shell omafan toggle` opens the panel; screenshot + transcription confirms the rendered panel (header, FIRMWARE AUTO, six preset chips with rpms, the floors footnote, RPM slider, footer). No QML error in `qs log` for any omafan file.
- 2026-09-15T04:02Z orchestrator defect report → T04c + T09b dispatched — the fast-path status document reports `thermal.t_eff_c: null` (state.json has 54.0) and `daemon.uptime_s: null`, so the bar falls back to its glyph and the header loses the temperature; the footer renders `Uptime · Polls 4124 · Verified: no`, which reads as a fault when no hold is active. T04c fixes the document, T09b the rendered strings.
- 2026-09-15T04:03Z orchestrator T04 INCIDENT (worker rule 4 violation, self-disclosed) — T04 ran `omafan-ctl preset low` once without `--pkexec none`, so pkexec applied a real hold on this machine while `t_eff` was 90 °C and the firmware was at 4794 rpm; the worker restored auto immediately. Orchestrator verified independently: `/run/afanctl/state.json` and `afanctl status --json` both show `mode=observe`, `manual=false`, `target_rpm=null`, `recent_errors=[]`. No lasting change; recorded here because a worker touching real hardware is exactly what rule 4 forbids. Lesson recorded in docs/BUILD-LOG.md.
- 2026-09-15T04:20Z orchestrator wave4 verified — `tests/run-all.sh` → PASS suites 6 / FAIL suites 0 (plugin-validate, manifest, model 148 asserts, ctl, keybindings, qml-lint); `tests/integration-shell.sh` and `tests/hw-smoke.sh` both refuse cleanly without OMAFAN_LIVE/OMAFAN_HW; every shipped shell file passes `bash -n`. README + INSTALL + TROUBLESHOOTING landed (T11), integration/hw scripts landed (T10).
- 2026-09-15T04:20Z orchestrator LIVE keybindings (PRD K2/K3/G7) — `bin/omafan-keybindings install` from the repo then from the installed copy; all eight chords appear in `hyprctl binds -j` with their omafan descriptions (modmask 72); `status` correctly reported `stale-path` when the block named the repo helper and `install` rewrote it to the installed path; `remove` restored `bindings.lua` **byte-identically** (md5 3b5174804388… before and after) and the eight binds disappeared. Key delivery itself was not simulated (Hyprland 4.0 here accepts only Lua dispatchers; `hl.dsp.send_shortcut` targets a window, not the bind table), so the chords are verified as registered + their commands verified by running them.
- 2026-09-15T04:22Z orchestrator BLOCKER (live, reproduced) — `omafan-ctl` built its privileged argv as `pkexec env AFANCTL_RUNTIME_DIR=… /usr/bin/afanctl hold <rpm>`. afanctl's polkit rule re-checks `program == /usr/bin/afanctl` and the exact argv, so the `env` wrapper makes it NOT_HANDLED and pkexec falls back to a password prompt: `pkexec /usr/bin/afanctl status --json` returns in 0.026 s, the `env` form hung until killed (timeout 124). Every write through the runner was therefore broken; only `--pkexec none` (the fixture path) ever worked, which is why the suite was green. T04d fixes the argv, refuses a custom runtime dir with a real runner, bounds every runner call, and adds the `pkexec_write_path` doctor check. T09c makes the panel survive a runner that never answers (kill + banner instead of an infinitely busy UI).
- 2026-09-15T04:22Z orchestrator hardware observation (afanctl-side) — with a bare `pkexec /usr/bin/afanctl hold 7200` accepted and held (target 7200, fan 7195 rpm at 83 °C), the daemon later reported `target_rpm 6200` while still `mode=hold` at 94 °C: afanctl's own over-temperature escalation writes `config.curve.max_rpm` (6200), i.e. it can move a hold *down*. omafan renders the daemon's numbers, so the panel showed 6,200 rpm — correct behaviour on our side, but worth telling the operator that afanctl's escalation outranks a hold. Fan released to `observe`/`manual=false` afterwards (verified).
- 2026-09-15T04:40Z orchestrator verified T04d (BLOCKER FIXED) — `preset full --dry-run --json` → argv `["/usr/bin/pkexec","/usr/bin/afanctl","hold","7200"]` (no `env`); a custom `--runtime-dir` with a real runner refuses with exit 2 and a message naming both fixes; `doctor` reports `pkexec_write_path PASS — polkit authorises /usr/bin/afanctl status --json in 23 ms`.
- 2026-09-15T04:45Z orchestrator REAL hardware write through the fixed runner (PRD G8) — `omafan-ctl preset high --notify` (the exact `SUPER+ALT+H` command) returned `ok` in **0.210 s** with the bare argv, the daemon reported `mode=hold, manual=true`, and the fan moved 7198 → 5741 rpm; `preset auto --notify` then returned `observe`, `manual=false`, fan back to 7130 rpm. Machine left owned by the firmware. Note: `target_rpm` read 6200 rather than the requested 5700, i.e. afanctl's own escalation wrote `curve.max_rpm`; omafan rendered that faithfully (`hold.preset: custom`).
- 2026-09-15T04:46Z orchestrator accepted T09c (panel command deadlines) and dispatched T05b (deepseek) because T04d intentionally invalidated four `tests/ctl.test.sh` assertions (doctor's check count, the §4.4 id set — now twelve with `pkexec_write_path` — and the old exit-3 expectation for a denied runner, which is now exit 2 before any runner call). T05b must also add the two regression guards: a stub runner that exits 42 if argv[1] is `env`, and a stub that sleeps 60 s to prove the new bounded wait yields exit 3 in under ~25 s. Suite: 187 PASS / 4 FAIL until it lands.
- 2026-09-15T04:50Z orchestrator FIXED + published — `tests/run-all.sh` → PASS suites 6 / FAIL suites 0 with `ctl` at 238 assertions (T05b added the two regression guards: a stub runner that fails if argv[1] is `env`, and a 60 s-sleeping stub that must yield exit 3 inside the bound). Repository pushed: https://github.com/yadav-prakhar/omafan (public, master, 70 files, GitHub detects GPL-3.0). `preview.png` added from a real screenshot of the live panel.
- 2026-09-15T05:00Z orchestrator read REVIEW-R2 (T16, glm 5.3) — verdict "REJECT until R2-2 is fixed: 1 blocker, 2 should-fix"; it independently found the polkit/argv blocker (R2-1, fixed by T04d while it ran) and the red gate (R2-4, fixed by T05b), and reported R2-2 (release_after_minutes can never fire), R2-3 (writes succeed untruthfully on a stale state.json), R2-5 (T04d/T09c drift un-mirrored in DESIGN.md).
- 2026-09-15T05:05Z orchestrator ruling R7 + fixes (REVIEW-R2 R2-2, R2-3, R2-5) — Panel.qml's release net is now armed on the hold's rising edge and restarted only by genuine user action (a one-shot timer restarted every poll could never reach its interval; the old DESIGN.md §8 sentence prescribed the bug and is corrected); bin/omafan-ctl refuses a write on a stale state.json (exit 5, `--force` overrides) — verified: state touched 2 min ago → exit 5, no argv.log, `--force` writes, fresh state writes; DESIGN.md §4 (runner argv, bounded calls, stale refusal, `pkexec_write_path`) and §4.4/§8 updated; DEVIATIONS.md rewritten with R1-R7 and the two orchestrator-applied worker-file fixes. Gate after the fixes: 6/6 suites PASS, qml-lint clean.
- 2026-09-15T05:15Z orchestrator closed the review nits (R2-6/7/8) — the panel's heat confirmation now sends the narrow `--force-undercooling` (verified: at a 90 °C/4794 rpm state `preset low` exits 8, `--force-undercooling` succeeds, and it still refuses exit 7 out-of-band and exit 6 on the degraded latch, while plain `--force` overrides all three); `--config` with a real runner is refused with exit 2 (R2-7) alongside the runtime-dir refusal; a missing `jq` now fails in the documented error style (exit 1 with the fix) instead of a raw 127, while `omafan-ctl version` still works without jq (R2-8). R2-9 (the T15 review never produced `REVIEW-R1.md`) is recorded as an open item in `docs/BUILD-LOG.md`. Gate after all fixes: 6/6 suites PASS, qml-lint clean, `bash -n` clean.
